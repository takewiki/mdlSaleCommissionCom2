

mdlSaleCommission2Server::server_saleOutStock_mfg_click(input=input,output = output,session = session,erp_token = erp_token,dms_token = dms_token)

mdlSaleCommission2Server::server_saleOutStock_mfg_view(input=input,output = output,session = session,dms_token = dms_token)






#管理成本查询，需要修改为生产成本
# mdlSaleCommissionServer::mngrCost_Server(input=input,output = output,session = session,dms_token = dms_token)





mdlSaleCommission2Server::promotion_server(input=input,output = output,session = session,dms_token = dms_token)





mdlSaleCommission2Server::promotionEmp_server(input=input,output = output,session = session,dms_token = dms_token)



