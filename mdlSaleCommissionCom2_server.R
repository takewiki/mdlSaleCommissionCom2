
mdlSaleCommissionServer::server_saleOrder_click(input=input,output = output,session = session,erp_token = erp_token,dms_token = dms_token)
mdlSaleCommissionServer::server_saleOrder_view(input=input,output = output,session = session,dms_token = dms_token)

mdlSaleCommissionServer::server_saleOutStock_click(input=input,output = output,session = session,erp_token = erp_token,dms_token = dms_token)
mdlSaleCommissionServer::server_saleOutStock_view(input=input,output = output,session = session,dms_token = dms_token)

mdlSaleCommissionServer::mngrCost_Server(input=input,output = output,session = session,dms_token = dms_token)

mdlSaleCommissionServer::promotion_server(input=input,output = output,session = session,dms_token = dms_token)

mdlSaleCommissionServer::promotionEmp_server(input=input,output = output,session = session,dms_token = dms_token)



